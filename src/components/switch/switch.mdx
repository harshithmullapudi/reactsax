---
name: Switch
route: /switch
---

import { Playground, Props } from 'docz';
import Switch from './index';

import '../../docz/common.scss';

# Switch

## Props

<Props of={Switch} />

## Default

Generate a switch element easily with beautiful animations and functionality

<Playground>
  <section className="d-section">
    <div className="d-section__item">
      <Switch circle />
    </div>
    <div className="d-section__item">
      <Switch circle /> 
    </div>
    <div className="d-section__item">
      <Switch loading circle /> 
    </div>
  </section>
</Playground>

## Color

Change the color of the component when it is in active state, the allowed values ​​are (main colors of vuesax, RGB, HEX)

<Playground>
  <section className="d-section">
    <div className="d-section__item">
      <Switch color="success" checked /> 
    </div>
    <div className="d-section__item">
      <Switch color="danger" checked /> 
    </div>
    <div className="d-section__item">
      <Switch color="warn" checked /> 
    </div>
    <div className="d-section__item">
      <Switch color="dark" checked /> 
    </div>
  </section>
</Playground>

## Text

Add a text to the switch with the default slot or if you need a different text for each state of the component you can use the `on` or `off` slots

<Playground>
  <section className="d-section">
    <div className="d-section__item">
      <Switch onText="Unsubscribe" offText="Subscribe" />
    </div>
    <div className="d-section__item">
      <Switch onText="off" offText="on" />
    </div>
    <div className="d-section__item">
      <Switch onText="Unselect" offText="Select" />
    </div>
  </section>
</Playground>

## indeterminate

Add an undetermined state to the compound with the `indeterminate` property, the property is a `boolean` so you can add it without any value.

<Playground>
  <section className="d-section">
    <div className="d-section__item">
      <Switch indeterminate />
    </div>
    <div className="d-section__item">
      <Switch indeterminate checked />
    </div>
  </section>
</Playground>

## Square

Change the circular style to square with the `square` property, the property is a `boolean` so you can add it without any value.

<Playground>
  <section className="d-section">
    <div className="d-section__item">
      <Switch square />
    </div>
    <div className="d-section__item">
      <Switch square checked /> 
    </div>
    <div className="d-section__item">
      <Switch square disabled /> 
    </div>
  </section>
</Playground>
